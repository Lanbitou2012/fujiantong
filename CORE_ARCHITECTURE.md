# 附件通 — 项目核心控制文档（系统宪法 V2.8）

> **⚠️ 开发前置警告（Development Rule）**
> 本文档是"附件通"项目的最高指导原则。任何数据库设计、API 开发、UI 绘制前，必须先读懂本文档。开发过程中如发现业务/规则变更，**必须先更新本文档再改代码**。
>
> **V2.0 重大修订说明**：V1.0 中"系统仅做记账，不走线上自动分账 API"的判断**完全错误**——微信官方提供"流量主代运营"机制，腾讯每半月自动将广告收益**按比例分别打款给平台和作者各自的银行账户**。这是本项目的根基性事实。
>
> **V2.1 修订说明（基于市场与官方文档对抗性验证）**：
> 1. **客群定位锁定为"个人运营者"**——即使作者背靠机构公众号，实际对接人也是个人，附件通的小程序统一以个人主体注册（用 `fastregisterpersonalmp`），机构主体不强求。
> 2. **公众号关联豁免**——附件复制的形式必须是完整 `mp-miniprogram` HTML 直达卡片，**作者无需在自家公众号后台做"关联小程序"**，这是回避"非同主体名额受限"风险的关键设计。（⚠️ **V2.7 已校正**：实际主链路是 `<a class="weapp_text_link js_weapp_entry">` 文本超链接，不是 `<mp-miniprogram>` 卡片。详见 V2.7 修订说明。）
> 3. **删除"企业版一键代注册"幻想**——`fastregisterweapp` 仅允许创建线下类目，附件下载属于线上工具类，**不能走这条路**。
> 4. **新增第 11 节竞品分析**与**第 12 节已识别业务风险表**，作为长期决策依据。
>
> **V2.2 修订说明（角色模型重塑）**：
> 1. **作者与推广员不是两类独立人**——推广员**本身就是一个作者**，只是在他自己的工作台里多出一个"推广收益"模块。所有 B 端用户共用一张 `users` 表 + `is_promoter` 叠加位，**绝不建独立的 promoters 表**。
> 2. **前期推广员由主理人手动添加**，架构同时预留"作者发布满 N 个附件 / 累计 M 元收益后自动开通"的成长路径开关（暂不开放）。
> 3. **小程序端 = 统一的作者工作台**——根据 `is_promoter` 标志位动态展示/隐藏"推广收益" Tab，**不做角色分离的两套界面**。
>
> **V2.3 修订说明（角色模型加固与防错乱铁律）**：
> 1. **推广员获取机制改为"申请制"**——仅当作者满足 N/M 门槛**且**主理人审核通过后才获得推广员能力，避免自动升级风暴。未达门槛的作者**看不到**申请入口。
> 2. **新增 3.7 节"角色模型铁律"**——列出 8 条防错乱规则，作为代码实现红线（推广收益按曝光日归属、单层提成不多层、角色变更必入历史表、未生效封锁等）。
> 3. **明确主理人最高优先级的人工控制闸**——推广员的身份获取、变更、降级均需主理人人工介入，不存在自动化升级/降级路径。
>
> **V2.4 修订说明（MVP 范围与全自动化路线图）**：
> 1. **明确运营约束：平台运营全部由主理人一人承担**，因此 MVP 需要**最大限度自动化**、主理人只保留"审核 + 打款"两个动作。
> 2. **基于微信官方完整 API 矩阵，运营端全链路自动化**：`getAuthorizerList` / `api_getsettlement` / `api_getadposdetail` / `AgencyCreateAdunit` 等接口足以支撑授权同步、收益拉取、广告位配置、佣金计算、结算单生成全流程自动化。
> 3. **新增第 15 节"第一期 MVP 范围与迭代路线图"** ——开发期"圣经页"，明确 P0/P1/P2 三层划分、自动化数据 Pipeline、上线节奏、铁律执行表。任何开发动作前必须先核对该节。
> 4. **作者入驻采取"推广员人肉协助 + 半自动"路线**：MVP 不集成 `fastregisterpersonalmp` 接口，作者本人在公众号后台复用主体创建小程序，推广员远程指导；V1.5 再接入自动注册。（⚠️ **V2.8 已精细化**：「复用公众号主体」会导致机构公众号作者拿到机构主体小程序，与「个人主体」默认不兼容。V2.8 改为作者在 `mp.weixin.qq.com` **自助注册个人主体小程序**，不会注册的由推广员远程人工协助。详见 V2.8 修订说明与 5.1 节。）
>
> **V2.5 修订说明（模板小程序全生命周期自动化）**：
> 1. **服务商身份能力充分挖掘**：作者一次授权后，平台自动完成该作者小程序的 「配置 → 代码部署 → 提审 → 发布 → 上线」全流程，主理人只在 「驳回处理 + 作者资质问题」才介入。
> 2. **模板代码 1 套、配置 N 套**：不同作者部署同一份模板代码，差异化（appid 、广告位 ID、作者品牌名、主题色等）全部走 `ext_json` 注入，模板 bug 修复一次推送全体。
> 3. **在第 15 节新增 15.2.8 节「模板小程序生命周期管理」**，增加 `mp_deployments` / `mp_audits` / `mp_template_versions` 三张表，集成 `wxa/commit`、`wxa/submit_audit`、`wxa/release` 等 12 个接口。
> 4. **升级 14 节待补齐项**：新增「作者授权链接需同时包含代码管理权限集」、「个人主体作者首次提审驳回率实测」、「隐私保护指引黄金版本过审」三个验证项。
> 5. **上线节奏调整 11 周 → 12.5 周**，W3 扩展为「W3-W3.5 微信 API SDK + 小程序生命周期 SDK」。
>
> **V2.6 修订说明（类目校准 + 证据等级铁律）**：
> 1. **类目修正**：V2.5 中「工具→效率工具」是 AI 基于网友帖子的臆造分类。**经主理人在微信公众平台后台实测**——个人主体小程序可正常审批通过「**工具→办公**」「**工具→信息查询**」「**商业服务→企业管理**」等类目。本项目模板小程序锁定主营类目为 **工具→办公**。
> 2. **证据等级铁律确立**：项目所有「官方支撑」判断必须来自 **A 级证据**（mp.weixin.qq.com 官方接口文档 / developers.weixin.qq.com 署名「微信团队」公告 / 主理人后台实测）。B 级（社区开发者帖）仅作线索，**不可作为推翻设计的依据**。C 级（网友投诉/自媒体）禁用。
> 3. **R4 风险降级**：「个人小程序工具类目审核」由 V2.5「具体审核标准未公开」校准为「已实证可批，但首次提审驳回率仍需 5-10 个种子作者实测」。
>
> **V2.7 修订说明（HTML 直达机制工程实证校准）**：
> 1. **主链路从 `<mp-miniprogram>` 卡片校准为 `<a class="weapp_text_link js_weapp_entry">` 文本超链接**。本项目从 V2.1 到 V2.6 一直在示例中写错了公众号文章正文嵌入小程序的真实机制。实际上公众号 Web 编辑器识别的是带 `weapp_text_link js_weapp_entry` 两个 class 的 `<a>` 标签，粘贴后自动渲染为蓝色可点击的小程序文本超链接。`<mp-miniprogram>` 标签用于公众号自动回复、服务号推送等辅助场景，不是文章正文路径。
> 2. **不同主体嵌入生死点被工程实证打破**：主理人提供的已有项目代码路径 `e:\项目\小项目\公众号附件助手\admin\src\views\author\AuthorFiles.vue:341-376` 已跑通整条「一键复制 → 粘贴 → 变蓝色超链接」链路：ClipboardItem API、contentEditable + execCommand('copy')、纯文本三级回退。**V2.6 中「不同主体公众号文章嵌入个人主体小程序卡片」生死点取消**。
> 3. **一键复制三个场景锁定为项目标准**（详见第 4.5 节）：
>    - 主路径：**一键粘贴**（`weapp_text_link` 文本超链接）——粘贴到公众号文章正文。
>    - 辅助路径：**小程序卡片代码**（`<mp-miniprogram>` 标签）——公众号自动回复、服务号推送。
>    - 兼底路径：**H5 备份链接**（`https://域名/f/文件ID`）——文章末尾备用下载入口 + 老版本微信用户。
> 4. **MVP 不是从零开始**：附件通上一代项目「公众号附件助手」已有 Vue 3 + Element Plus 实现的作者附件列表页，含「一键复制」完整逻辑。**本项目可复用该代码作为后台起点**，重点加入：多租户作者体系、服务商完整接入、推广员提成、收益拉取与结算、模板小程序生命周期管理。
>
> **V2.8 修订说明（入驻路径终极锁定 + V2.7 后通读校对）**：
> 1. **入驻路径终极锁定为「作者自助注册 + 自动化授权」**：作者自己在微信公众平台注册个人主体小程序（按平台提供的图文/视频指引），不会注册的由推广员远程人工协助（屏幕共享/电话指导）。**附件通不集成 `fastregisterpersonalmp` 接口**，不做「扫码代注册」。作者注册完成后只需把 appid 回填到 H5，后续授权 + 配置 + 部署 + 提审 + 发布全自动化。**这是 V2.4 决策的精细化版本**，并与 V2.7 「服务商能力最大化」理念兼容。
> 2. **5.1 节五步法重写**：原 ③ 步的 `fastregisterpersonalmp` 一键代注册内容取消，替换为「作者自助注册（推广员可协助）+ appid 回填」两步。同步在 5.1.1 明确「小程序注册无法自动化是已知约束，不算缺陷」，5.1.1 表中删除原「`fastregisterpersonalmp` 小程序不可绑定微信支付」不适用项。
> 3. **通读校对修正 14 处描述失准与笔误**：包括 V2.1 修订说明中 mp-miniprogram 残留描述加 V2.7 校正标注、修订说明版本顺序调整（V2.6 现提到 V2.7 之前）、R3/R4 表格字符乱码修复、接口名 `GetSettlement`→`api_getsettlement` 统一、3.2/10/11.2 节「HTML 卡片」表述改为「一键复制（文本超链接）」、W9 周生死点同步刷除、5.1 准实名认证表述从「fastregisterpersonalmp 收集」改为「作者在微信公众平台自助完成」、接口表 fastregisterpersonalmp 加「V1.5 接入」标注、3 处笔误修复、底部版本控制补 V2.6/V2.7/V2.8 条目。

---

## 1. 项目定位与核心愿景

- **产品名称**：附件通
- **核心痛点**：微信公众号文章无法直接挂载附件（政企岗位表、报名表、通知文件等）。
- **产品形态**：PC Web 端（主办公台）+ 微信小程序（C 端变现 + B 端移动协同）。
- **核心愿景**：以极简上传/复制体验圈住 B 端公众号作者，借助微信"流量主代运营"机制把 C 端读者的下载流量自动变现，实现平台、作者、推广员三方共赢。

### 1.1 目标客群定位（V2.1 锁定）

**默认作者 = 个人运营者**，分为两类：

| 客群 | 描述 | 占比预期 | 入驻优先级 |
|---|---|---|---|
| **小作者** | 个人订阅号运营者；公众号本身就是个人主体；附件少、阅读量小 | ~70% | ⭐⭐⭐⭐⭐ 主力客群 |
| **机构运营者个人** | 受雇于企业/机构、实际运营某个机构公众号；公司不主动碰这事，运营者用自己身份做小程序、独享 72% 分成 | ~25% | ⭐⭐⭐⭐ 核心增长点 |
| ~~大公司公关账号~~ | 公司法务流程长、不一定愿意接"流量主代运营"协议 | ~5% | ❌ **暂不主推** |

**核心判断**：附件通的所有小程序**统一用作者个人身份**（身份证 + 人脸识别）通过 `fastregisterpersonalmp` 注册。这是产品定位的基石，绕开所有"企业主体代注册"的死路。

---

## 2. 平台已落地的不可变事实（V2.0 输入条件）

下列事实在 V2.0 立项时已经成为既定输入，不再讨论：

| 项 | 值 |
|---|---|
| 服务商主体 | 河南乐享数字生活服务有限公司（一般纳税人，可开增值税专用发票）|
| 第三方平台 AppID | `wx037bc87a7245b35e`（平台型）|
| 平台对外品牌 | 附件通 |
| 服务商域名 | `fujian.5g6g.top`（开发期临时；上线前需替换正式域名）|
| "流量主代运营"权限集 | 已开通（权限集 ID = **135**）|
| 服务商结算账户 | 已审核通过；结算单邮箱 `552002521@qq.com` |
| 平台向腾讯开票 | 增值税专用发票，开给"腾讯科技（深圳）有限公司"，货物名称可选"信息技术服务费/技术服务费/微信推广费/服务费" |

---

## 3. 系统角色与权限边界（V2.2 重塑：基础角色 + 叠加权限）

### 3.1 角色模型一图概括

```
C 端粉丝（独立人群，仅消费内容，无后台）
        │
        │  完全分离，互不交叉
        ▼
─────────────────────────────────────────────
B 端用户（所有 PC Web / 作者工作台用户）
  统一存储在 users 表，统一登录方式（微信扫码）
        │
        ├─ 基础角色（必有）  ：  作者
        ├─ 叠加权限 ①（可选）：  is_promoter = true（推广员能力）
        └─ 叠加权限 ②（可选）：  is_admin    = true（平台管理员能力）
```

**关键原则**：B 端不存在"只是推广员、不是作者"的人。**任何推广员一定也是作者**（持有自己的小程序、可以发自己的附件、可以拿自己的流量主分成）。

### 3.2 三种 B 端身份的具体含义

| 身份 | 字段组合 | 在系统里做什么 |
|---|---|---|
| **普通作者**（默认）| `is_promoter=false, is_admin=false` | 上传附件、一键复制（文本超链接/小程序卡片/H5 备份）、查看自己 72% 的流量主收益 |
| **作者 + 推广员** | `is_promoter=true,  is_admin=false` | 普通作者所有能力 + 持专属二维码 + 看名下作者列表与推广收益结算 |
| **作者 + 管理员**（主理人）| `is_promoter=*,     is_admin=true`  | 全平台后台权限：配 share_ratio、模板版本、违规处置、生成结算单、标记已转账等 |

**主理人通常 `is_promoter=true` 同时 `is_admin=true`**——既能自己发附件、又能拉作者、又能管平台。

### 3.3 C 端粉丝（与 B 端完全分离）

- 点击公众号文章附件卡片 → 打开作者的小程序 → 触发激励/插屏广告 → 预览/下载文件。
- **绝对禁止**在小程序里向 C 端粉丝展示文件管理、后台、收益等任何 B 端入口。
- C 端用户在 `users` 表里**不存在**——只在小程序广告统计 / UV 数据中体现。

### 3.4 推广员能力的获取方式（V2.3 改为申请制）

**前期（仅依赖人工添加）**：由主理人在平台后台直接将某个作者的 `is_promoter` 切换为 `true`，并生成专属推广二维码/短链。作者侧看不到任何申请入口。

**预留（暂不开放的申请制机制）**：当作者满足任一条件后**解锁申请资格**（仅解锁按钮，不自动升级）：
- 累计上传附件数 ≥ N（N 由后台配置，建议初值 20）；
- 累计流量主到手收益 ≥ M 元（M 由后台配置，建议初值 500）。

**以上仅是"解锁申请按钮"，不是"自动升级"**。完整申请流程：

```
不满足门槛                  →  作者看不到申请入口
满足门槛（解锁态）        →  小程序工作台出现"申请成为推广员"按钮
作者点击申请（待审核）    →  主理人后台收到申请单（含快照统计数据）
主理人审核                  →  通过：is_promoter=true →  拒绝：30 天冷却期
```

**降级**：仅主理人后台可手动取消推广员身份；已绑定的下级作者关系**保留**，但降级当日及以后不再产生新的推广分成（详见 3.7 节铁律 1）。

### 3.5 用户成长闭环（产品自然裂变机制）

```
①  路人作者被推广员邀请 → 入驻 → 发首篇附件
②  小程序工作台出现"我的流量主收益"，逐月累积
③  累计满足门槛 → 解锁"申请成为推广员"按钮 → 作者主动申请
④  主理人后台审核 → 通过后授予推广员能力
⑤  小程序工作台新增"我的推广收益" Tab + 专属二维码
⑥  作者把链接分享给同行 → 拉来新作者
⑦  推广收益开始入账 → 形成正反馈循环
```

这是"附件通"产品的**核心增长飞轮**：**作者用着用着自然变成推广员，主理人始终控制推广员质量闸门**。

### 3.6 小程序端 UI 实现规则

**所有 B 端用户在小程序内进入的是同一个"作者工作台"**，根据用户的 `is_promoter` + 申请资格动态展示模块：

```
作者工作台
├─ 我的附件                  [所有作者]
├─ 我的流量主收益             [所有作者]
│   └─ 数据：我自己 72% 的部分（来自我自己小程序的广告）
├─ 我的推广收益  ⭐           [仅 is_promoter=true 才显示]
│   ├─ 名下作者列表与各自本月收益
│   ├─ 本月推广分成（= Σ 名下作者流量主毛收益 × 28% × 30%）
│   └─ 历史结算单（已转账/待转账）
├─ 申请成为推广员  ⭐️       [仅满足门槛且 is_promoter=false 且 未在冷却期才显示]
├─ 设置（绑定银行卡、查看协议）  [所有作者]
```

**禁止**为推广员单独做一个小程序或单独的工作台页面。

### 3.7 角色模型铁律（V2.3 防错乱红线清单）

以下 8 条是贯穿所有代码实现的红线，开发/调试/审查时**每违反一条必须重构**：

**铁律 1：推广收益按「广告曝光日」逐日归属，不按月批量**
- 下级作者某日产生的流量主毛收益 × 28% × 30% → 该日作者的 parent。
- parent 那日是推广员才计，那日不是推广员则不计（例如降级后的曝光不再计）。
- 这要求 `is_promoter` 状态变更必须写入历史表（见铁律 7）。

**铁律 2：推广分成仅算单层，绝对禁止多层传销结构**
- 计算公式仅依赖 `parent_promoter_user_id`，不允许递归向上查找"上上级"。
- 这是《禁止传销条例》的合规红线（三级及以上为重点打击对象），不可谈判。

**铁律 3：数据库必须 CHECK 防止自引用，业务层递归验证防循环**
- 表级约束：`CHECK (id <> parent_promoter_user_id)`
- 代码级验证：修改 parent 前**递归向上 walk**，遇到自己则拒绝（即使是主理人手动修改也拒绝）。

**铁律 4：后端中间件每次实时校验权限，不信任 session/JWT 快照**
- `/api/promoter/*` 路由必须在中间件中 **SELECT users.is_promoter** 实时查，不能仅看 token 里的快照值。
- 权限被主理人修改后，后端起动可选「kick all sessions of user_id」动作踢下线。

**铁律 5：主理人自身的推广分成不跳过计算，但财务报表单独打标**
- 主理人 M（`is_admin=true 且 is_promoter=true`）名下有作者时，推广分成照常计算。
- 结算表里该记录打标 `is_self_promoter=true`，财务对帐时以"主理人个人劳务收入"名义出帐。

**铁律 6：推广员申请被拒后有30天冷却期，冷却期间申请按钮置灰**
- 防止被拒作者重复提交损耗主理人精力。
- 冷却期实现：查 `promoter_applications` 表最近一次 status=rejected 的记录，`rejected_at + 30天 > now` 则置灰。

**铁律 7：`is_promoter` 与 `is_admin` 变更必须写入历史表**
- 专表 `user_role_history`：`user_id, field, old_value, new_value, changed_by_admin_id, changed_at, reason`
- 主表 `users` 里的标志位反映「当前状态」；历史表反映「什么时间为什么是什么状态」。
- 结算逻辑查历史表判定「某日该用户是否是推广员」，这是铁律 1 的必要支撑。

**铁律 8：申请提交时快照统计数据，审核期间不受业务波动影响**
- `promoter_applications` 记录写入时映下 `snapshot_upload_count`、`snapshot_total_revenue`、`snapshot_at`。
- 主理人看到的始终是申请那天的数据，避免「审核中作者下架附件到不达标但审核还能过」这种错乱。

---

## 4. ⭐ 商业模式与官方分账机制（V2.0 核心）

### 4.1 资金流（铁律）

```
广告主投放 100 元
        ↓
微信广告平台扣 50% 渠道技术服务成本（腾讯固有规则，平台无关）
        ↓
剩 50 元 = "广告变现收入"（这就是平台 + 作者 共同分的总盘子）
        ↓
微信广告按 share_ratio 自动拆账，分别打到两个银行账户
        ├── 28%（14 元）→ 平台（河南乐享）对公账户
        └── 72%（36 元）→ 作者个人/个体/企业账户
        ↓
每半月一次（次月 1 号、15 号前发结算单到邮箱）
```

**关键点**：
- 平台**不需要自建分账系统**——腾讯就是分账的执行方。
- 平台和作者**互不接触资金**——彼此从腾讯各自拿钱，互不开票，互不打款。
- "28%/72%"是腾讯结算给两方的金额比例，**正好对应官方 `share_ratio = 28`**（服务商占比）。

### 4.2 share_ratio 配置规则（必须写进运营 SOP）

- **默认比例**：调用官方 `SetShareRatio` 接口设为 **28**（服务商占 28%）。
- **❗ 致命陷阱**：若服务商未主动设置默认比例，官方默认为 0:10（服务商分到 **0 元**）。**平台上线第一件事必须先设默认 28**。
- **差异化比例**：可针对单个作者另设比例（如重点合作伙伴 15%、维护成本高的作者 35%），优先级高于默认比例。
- **生效条件**：服务商设置后**不立即生效**；商家在"流量主代运营"权限集授权流程中**手动勾选确认协议**后，**次日**生效。
- **变更**：调整比例需要作者重新走"更新授权"流程，否则不生效。

### 4.3 推广员佣金（平台内部分润，与腾讯无关）

- 推广员佣金来源 = **平台从腾讯拿到的 28% 中切 30%** → 即作者流量主收益总盘的 `28% × 30% = 8.4%`。
- **腾讯不参与推广员分账**——推广员佣金由平台从对公账户人工/半自动转账到推广员个人。
- 系统职责：
  - 自动记账（作者-推广员绑定关系、每日广告收益、推广员应得金额）。
  - 生成"推广员结算单"（月度，含明细 + 总额）。
  - 管理员一键勾选"已转账" → 系统标记结算完成。
- **不接微信支付分账接口**——金额小、对象散、合规风险低，人工转账成本最低。

### 4.4 三类作者主体的差异（产品文案需告知）

| 作者主体 | 腾讯结算方式 | 是否需作者开票 | 产品提示文案 |
|---|---|---|---|
| 个人 | 腾讯直接打到银行卡，**已代扣代缴个税** | 不需要 | "微信将每半月自动打款到您的银行卡，已为您代扣代缴个税" |
| 个体工商户 | 可选"委托个人收款 + 平台代征代扣"通道（推荐）；或走正常开票 | 走委托通道则不需要 | 推荐"委托代征代扣"，操作最简 |
| 企业 | 作者需向腾讯邮寄等额增值税发票后 30 工作日打款 | **必须开票** | 明确告知开票流程，否则钱拿不到 |

**产品策略**：基于 1.1 节客群锁定，**附件通统一引导作者注册个人主体小程序**（即使作者公众号是机构主体）。这同时满足：
- 接入流程最简（无营业执照、对公账户、法人信息收集）；
- 结算最简（腾讯直接打款 + 代扣代缴个税）；
- 与官方 `fastregisterpersonalmp` 接口能力完全对齐。

### 4.5 ⭐ 公众号关联豁免设计（V2.1 核心机制・V2.7 工程实证校准）

**问题**：作者公众号若是机构主体，与附件通注册的个人主体小程序属于"非同主体"——公众号最多关联 3 个非同主体小程序，且每次关联需要小程序方扫码确认。如果走此通路，将极大增加作者操作成本。

**官方明确条款**（微信团队 2019.04.04 公告原文）：「图文消息中可直接使用小程序卡片、链接、图片素材，**无需关联小程序**。」

#### 4.5.1 主链路：`weapp_text_link` 文本超链接（一键粘贴）

**原理**：公众号 Web 编辑器识别剪贴板中带特定 class 的 `<a>` 标签，自动渲染为蓝色小程序文本超链接。附件通在 PC Web 工作台将构造的 HTML 写入系统剪贴板，作者粘贴后即在文章里生成可点击的蓝色链接。

**附件通标准 HTML 结构**（与上代项目「公众号附件助手」已跑通代码一致）：

```html
<a class="weapp_text_link js_weapp_entry"
   data-miniprogram-type="text"
   data-miniprogram-appid="<作者小程序 appid>"
   data-miniprogram-path="pages/file-detail/file-detail?id=<文件ID>"
   data-miniprogram-nickname="附件通"
   data-miniprogram-servicetype="">
  岗位表2025.xlsx
</a>
```

**关键点**：
- `weapp_text_link` + `js_weapp_entry` 两个 class 是公众号编辑器识别小程序文本链接的标记
- `data-miniprogram-appid` 指向作者自己的小程序（附件通代注册的个人主体小程序）
- `data-miniprogram-path` 指向小程序内的文件详情页
- 标签内文本即为文章里呈现的蓝色链接文字（默认用文件名，可以自定义）

#### 4.5.2 剪贴板写入的三级回退策略（准生产实现）

代码细节参考 `e:\项目\小项目\公众号附件助手\admin\src\views\author\AuthorFiles.vue:341-376`：

1. **优先：ClipboardItem API**（Chrome/Edge 现代版本）——同时写入 `text/html` 和 `text/plain` 两种 MIME 类型
2. **回退：contentEditable div + execCommand('copy')** ——创建隐藏可编辑 div，注入 HTML，选中后执行复制，可保留富文本格式
3. **最终兜底：纯文本**——`navigator.clipboard.writeText` 或 textarea fallback

#### 4.5.3 作者操作流程

1. 作者在工作台文件列表点 **「📋 一键粘贴」**
2. 系统将上述 HTML 写入剪贴板
3. 作者切到公众号 Web 编辑器（`mp.weixin.qq.com`），光标定位到正文，**Ctrl+V**
4. 公众号编辑器识别到 `weapp_text_link` class → 自动渲染为 **蓝色小程序文本超链接**
5. 文章发布后，读者点击该蓝色链接 → 跳转到作者的小程序 → 打开文件详情页 → 查看/下载附件

**浏览器要求**：作者使用 Chrome/Edge 在 `mp.weixin.qq.com` 粘贴；Safari、Firefox 可能丢失富文本格式，入驻指引需明示该要求。

#### 4.5.4 辅助与兼底场景

| 按钮 | 生成内容 | 适用场景 |
|---|---|---|
| **📋 一键粘贴**（主）| `<a class="weapp_text_link js_weapp_entry">...</a>` | 公众号文章正文粘贴变蓝色超链接 |
| **复制小程序卡片代码** | `<mp-miniprogram>` 标签 | 公众号后台「自动回复」、服务号推送等辅助场景 |
| **🔗 H5 备份** | `https://<附件通域名>/f/<文件ID>` 纯链接 | 文章末尾备用下载入口；老版本微信 / 非微信下载入口 |
| **复制小程序路径** | `pages/file-detail/file-detail?id=<文件ID>` | 作者手动在公众号编辑器选择小程序卡片后手动填入路径的场景 |

**整条链路不需后端参与复制过程，纯前端剪贴板操作，零延迟。**

#### 4.5.5 为什么该机制跳出关联限制

- 公众号编辑器 GUI 里「插入小程序」按钮需要公众号已关联该小程序才能选择
- 但推文发布后的文章正文 HTML 中的 `weapp_text_link` 节点的点击动作仅依赖 `data-miniprogram-appid` 和 `data-miniprogram-path` 两个属性，微信客户端会直接拉起指定 appid 的小程序下的指定页面
- 这是附件通相对于市面竞品（fjjia/wzfj/微附件X 要求作者使用服务商统一小程序，作者拿不到流量主收益）的**结构性优势**：附件通是「每作者一独立小程序」模式，作者拿 72% 流量主收益，同时作者不需要在自己公众号上动任何关联。

**作者无需任何关联操作 = 入驻流程的真正自动化锚点。**

---

## 5. 作者入驻全自动化 SOP（小白零理解成本）

整体目标：作者**5 分钟内 + 全程在手机微信里**完成入驻、授权、上线广告。

### 5.1 标准入驻六步法（V2.8 锁定版）

| # | 步骤 | 实现技术 | 作者感知 | 耗时 |
|---|---|---|---|---|
| ① | 推广员发专属二维码/链接 | 平台后端生成带 `promoter_id` 的短链 | 扫码进入 H5 落地页 | 10 秒 |
| ② | 作者用本人微信扫码登录 | 微信网页授权（OAuth）| 微信授权 → 自动获取 unionid → 绑定 `promoter_id` | 10 秒 |
| ③ | **作者自助注册个人主体小程序**（V2.8 调整）| H5 内嵌「图文 + 视频」详细指引：作者使用未注册过小程序的微信号在 `mp.weixin.qq.com` 注册「小程序」→ 选「个人」主体 → 完成身份证 + 人脸 + 手机号实名认证。**不会注册的作者**：点 H5 「需要帮助」按钮→ 推广员后台收到请求→ 远程屏幕共享/电话指导作者完成。| 在 mp.weixin.qq.com 走完注册流程，拿到小程序 appid | 5-10 分钟（作者自助）或 15-30 分钟（推广员远程协助）|
| ④ | **回填 appid + 一键授权** | 作者把小程序 appid 填入 H5 表单 → 跳转微信官方授权页（同时勾选权限集 135 代运营 + 代码管理权限集 + share_ratio=28 协议）| 填 appid → 点「授权」→ 看到「授权附件通代运营流量主，分账比例 28% : 72%」→ 勾选协议 → 确认 | 1 分钟 |
| ⑤ | 绑定结算银行卡 | 引导跳转「微信广告助手」公众号 | 一次性填完 | 1 分钟 |
| ⑥ | **平台自动化部署**（无作者感知）| 平台收到授权事件 → SetShareRatio + AgencyCreateAdunit + wxa/add_category + wxa/modifyserverdomain + wxa/setMpPrivacySetting + wxa/commit + wxa/submit_audit → 审核通过后 wxa/release | 约 10-30 分钟后收到「小程序已上线」模板消息通知 | 10-30 分钟（自动）|

**铁律事实校验**：
- 作者自助注册的个人主体小程序默认为未认证状态。未认证不影响发布上架，但影响"被搜索/分享"。**本项目不依赖搜索曝光**（流量来源是公众号文章），未认证状态可接受。
- 个人主体小程序**不可绑定微信支付商户号**——本项目无支付需求，不影响。
- 作者授权链接必须**同时勾选流量主代运营权限集（135）与小程序代码管理权限集**，否则自动化流水线断。

**结果**：次日起腾讯按 28:72 自动分账；UV 满 1000 后流量主自动激活，广告即刻上线（模板已预埋）。

### 5.1.1 ⚠️ 路径不可行的反面警示（V2.1 定 + V2.8 校准）

以下"看似可行"的捷径**全部已被官方文档排除**，开发期不要再走回头路：

| 设想路径 | 实际限制 | 来源 |
|---|---|---|
| 用 `fastregisterweapp` 给作者**代注册企业主体小程序** | 该接口**只能创建线下类目**，附件工具属线上类目，会被驳回 + 影响 quota | [官方文档](https://wdk-docs.github.io/wxopen-docs/wxapp/fast-regeister.html) |
| 用 `fastregisterpersonalmp` 「扫码代注册」帮作者一键注册小程序 | **V2.8 决策不接该接口**。原因：作者人脸识别仅能在本人微信上进行，平台推送 H5 后体验警示多、作者悘挫率高；不如直接引导作者走「微信公众平台」正规注册入口（作者熟悉、推广员可远程协助）。**V1.5 可重新评估接入** | V2.4/V2.8 产品决策 |
| 通过"公众号复用主体快速创建小程序"**自动化** | 公众号管理员**必须本人**在微信公众平台后台操作；服务商不能代为完成。但**作者本人手动可选**该路径走「复用主体」需准备公众号管理员身份，机构公众号会复用出机构主体小程序，**与本项目「个人主体」默认不兼容** | 公众号关联小程序文档 |
| 让作者跳过"权限集授权"，平台单方面设 share_ratio 即生效 | 服务商设置后**不立即生效**；商家必须确认 135 权限集才生效 | `api_setshareratio` 官方文档 |

**V2.8 补充说明**：「小程序注册无法全自动化」是微信产品层面的安全约束（人脸需本人、身份证本人拍摄），**不算附件通项目的缺陷**；推广员远程人工协助为 MVP 的合理底线，升到 V1.5 后可评估是否引入 `fastregisterpersonalmp` 作为“扫码代注册”补充选项。

### 5.2 流量主开通门槛（UV > 1000）的 UX 兜底

- 新作者的小程序在 UV < 1000 期间**不展示任何广告**，纯粹做"附件下载工具"。
- 后台监控每个授权小程序的 UV 走势，逼近 1000 时给作者推送模板消息提示"即将开启变现"。
- 满 1000 时**自动**调流量主开通接口 + 创建广告位（无需作者操作）。

### 5.3 引导作者授权的两条渠道

- **主动渠道**：平台 Web 后台 / 公众号客服消息推送授权链接（参考案例：微盟）。
- **模板消息触达**：新功能，调接口触发模板消息直推商家管理员微信（参考案例：思迅）。

---

## 6. 技术架构与高并发演进路线（保留 V1.0 决策）

V1.0 的技术栈选型经分析仍最优，**保留不变**：

1. **端侧协同**
   - PC 优先：重度编辑、上传、富文本复制 → 100% 在 PC Web 端完成。
   - 移动轻管：小程序借 `wx.chooseMessageFile` 等原生能力做应急协同，不追求全功能。

2. **后端语言**：Go (Gin + GORM)
   - 抗并发：协程模型在 2C4G 上扛十万级瞬时下载洪峰。
   - 极简部署：编译产物为单一无依赖二进制，宝塔面板"进程守护"即可拉起。

3. **前端**：Vue 3（PC Web 控制台）+ 微信小程序原生框架（C 端 + B 端协同）。

4. **数据库与缓存**
   - MySQL 单库起步，但**所有业务表必须预留 `authorizer_appid`（租户字段）**，为分库分表与"按小程序隔离"做准备。
   - Redis 强制介入热点路径：文件下载链接、广告位配置、`share_ratio` 缓存。
   - 防击穿三件套（互斥锁 + 空值缓存 + 逻辑过期）必须实现。

5. **附件存储演进**
   - V1.0：本地服务器（跑通闭环、控成本）。
   - V2.0：无缝迁腾讯云 COS + 强制开 CDN。下载链接走 STS 临时凭证防盗链。

---

## 7. 流量主代运营接入清单（开发必读）

### 7.1 必须实现的官方接口

| 用途 | 接口 | access_token 类型 |
|---|---|---|
| 设置默认分账比例 | `POST /wxa/setdefaultamsinfo?action=set_share_ratio` | `component_access_token` |
| 查询分账比例（默认 + 单作者）| `POST /wxa/getdefaultamsinfo?action=get_share_ratio` | `component_access_token` |
| 设置单作者差异化比例 | （同上接口，传 appid + share_ratio）| `component_access_token` |
| 拉取已授权账号列表（每日）| `getAuthorizerList` | `component_access_token` |
| 获取授权账号详情 | `getAuthorizerInfo` | `component_access_token` |
| 快速注册个人小程序（**V1.5 接入**，MVP 不集成）| `fastregisterpersonalmp` | `component_access_token` |
| 流量主开通 / 广告位管理 / 广告管理 | `AgencyCreateAdunit` 等代运营 API 全套 | `authorizer_access_token` |
| 获取广告 & 结算数据 | `api_getsettlement` | `component_access_token` |
| 触发模板消息引导授权 | 模板消息触达授权（新功能）| 服务商平台调用 |

### 7.2 模板小程序策略（核心生产力武器）

**铁律**：模板小程序必须**预埋全部广告组件代码**，授权后无需重新发版即可上线广告。

- **预埋的广告位类型**（基于 `guide.txt`）：
  - 开屏：用户进入小程序时 5 秒广告（暂不接入，初期为提升体验）
  - 插屏（`wx.createInterstitialAd`）：Tab 切换、流程结束等场景，**主力变现位**
  - 激励视频（`wx.createRewardedVideoAd`）：下载/解锁文件前的"看广告解锁"，**收益最高**
  - 原生模板：横幅/格子/横版卡片，用于下载页底部
- **adUnitId 动态注入**：模板代码不写死 adUnitId，运行时通过 `getAccountInfoSync().miniProgram.appId` + 后端接口拉取当前小程序的广告位 ID。
- **配置化开关**：每个广告位是否启用、刷新频率、展示场景由平台后端下发，作者无感知。

### 7.3 接入流程（一次性）

服务商已完成第三方平台搭建（事实见第 2 节），后续每个新作者只走第 5 节的入驻 SOP。

---

## 8. 平台后台运营管理（必建模块）

### 8.1 作者维度看板
- 已授权作者列表（appid、主体类型、授权时间、当前 `share_ratio`、UV、本月广告收益）。
- 每个作者的：流量主开通状态、广告位配置、违规记录、绑定推广员。
- 一键调整某作者的 `share_ratio`（触发重新授权流程）。

### 8.2 推广员维度看板
- 推广员列表、名下作者数、本月作者总收益、推广员应得佣金。
- 月度结算单生成 + 一键标记"已转账"。

### 8.3 财务看板
- 服务商维度月度结算汇总（与腾讯邮件结算单对账）。
- 月度向腾讯开票辅助：列出本月应开票金额、自动生成开票申请。

### 8.4 风控
- 监控授权异常事件推送（小程序解绑、违规处罚回调）。
- 文件内容审核结果记录 + 人工复核入口。

---

## 9. 合规与风控基线

1. **作者强制实名**：通过微信扫码 + 注册 API 自带的实名认证，责任已转移到作者。
2. **文件内容自动机审**：`msgSecCheck`（文件名/简介文本）+ `mediaCheckAsync`（PDF/图片等附件）。
3. **审核未通过**的文件不生成下载链接、不可粘贴到公众号。
4. **隐私合规**：小程序首次启动展示《用户隐私保护指引》，符合个人信息保护法。
5. **域名 ICP 备案**：上线前确保正式域名已完成 ICP 备案并配置到第三方平台白名单。

---

## 10. 核心业务闭环数据流

1. **入驻流**：推广员二维码 → 作者扫码 → 自动注册小程序 → 流量主代运营授权 → 设 share_ratio 28 → 自动绑定推广员关系。
2. **生产流**：作者 PC Web 上传文档 → `mediaCheckAsync` 机审 → 一键复制（`weapp_text_link` 文本超链接 / `mp-miniprogram` 小程序卡片 / H5 备份链接三选一）→ 粘贴到推文。
3. **消费流**：粉丝阅读推文 → 点击附件卡片打开作者小程序 → 触发激励/插屏广告 → 解锁下载/预览。
4. **协同流**：作者手机收到正确文件 → 打开小程序工作台 → `chooseMessageFile` 选新文件 → 替换原文件（链接不变）→ C 端即刻生效。
5. **变现流**：广告曝光/点击 → 腾讯每半月按 28:72 自动分账到双方账户 → 平台向腾讯开专票 → 平台内部按"作者收益 × 28% × 30%"计算推广员佣金 → 人工转账给推广员 → 系统标记已结算。

---

## 11. 竞品分析与差异化定位（V2.1 新增）

### 11.1 直接竞品现状

| 竞品 | 模式 | 作者收益 | 平台收益 | 用户体验 | 防御性弱点 |
|---|---|---|---|---|---|
| **附件夹**（fjjia.cc）| 单一中心化小程序，所有作者共用 | **0 元** | 流量主收益 100% | 上传 → 复制链接 → 关联他们的小程序 → 插入推文 | 作者文件托管在竞品；竞品跑路则文件失效；作者无议价能力 |
| **文档 X / 微附件 X**（wzfj.cc）| 同上 | **0 元** | 同上 | 同上 | 同上 |
| **微附件**（早期产品）| 同上 | **0 元** | 同上 | 同上 | 同上 |

**共性**：现有竞品全部走"中心化小程序 + 作者免费 + 平台独吞流量主收益"路径。**作者本质上是"被白嫖流量"的角色**——为竞品的小程序贡献 UV，自己一分钱不拿。

### 11.2 附件通的结构性差异

| 维度 | 附件夹等竞品 | 附件通 |
|---|---|---|
| 小程序归属 | 平台所有 | **作者所有**（个人主体） |
| 流量主收益分配 | 平台 100% | 平台 28% / 作者 72% |
| 作者议价权 | 无 | **有**（小程序是作者的，可迁移、可独立运营）|
| 文件资产风险 | 平台跑路 = 文件全失效 | 作者授权可解除 = 文件可迁回作者自托管 |
| 公众号关联负担 | **必须关联**竞品小程序（占用关联名额）| **无需关联**（weapp_text_link 文本超链接直达）|
| 用户体验复杂度 | 注册 → 上传 → 关联小程序 → 复制链接 → 插入 | 入驻 → 上传 → 一键复制（文本超链接）→ 粘贴 |

### 11.3 差异化营销话术（推广员对作者）

1. **"你自己的小程序，你拿大头"**——这是市面唯一让作者拿到流量主分成的方案。
2. **"零关联负担"**——不抢你公众号的 3 个非同主体关联名额。
3. **"附件就是你的资产"**——小程序授权可随时解除，文件随时可迁出。
4. **"小白也能玩"**——5 分钟手机端完成全流程，无需理解技术细节。

---

## 12. 已识别业务风险与缓解策略（V2.1 新增）

| 风险 | 等级 | 描述 | 缓解策略 |
|---|---|---|---|
| **R1. 收益绝对值偏小** | 🟠 中 | 工具类小程序 eCPM 约 10-30 元/千次（激励视频）；小作者单月收益约 50-300 元，作者 72% 后到手 36-216 元。对小作者吸引力弱。| 推广话术不强调"赚钱"，强调"零成本拥有自己的小程序+附件托管+被动收益"；定位为"工具+顺带变现" |
| **R2. UV 1000 门槛慢** | 🟠 中 | 1 篇 1 万阅读推文 ≈ 1000 次附件下载 ≈ 1000 UV。意味着新作者发**1-2 篇推文**才能开通流量主。| UV < 1000 期间纯工具属性，不展示广告；后台监控 UV 接近门槛时推模板消息提醒 |
| **R3. 非同主体关联名额限制** | 🟢 极低（V2.7 已工程实证解决）| 公众号最多关联 3 个非同主体小程序 | 第 4.5 节 `weapp_text_link` 文本超链接机制上一代项目已跑通，不走关联通道，不受该限额影响 |
| **R4. 个人小程序工具类目审核** | 🟢 极低（V2.6 已实证）| 主理人后台实测：个人主体可批「工具→办公」「工具→信息查询」类目。残余风险为首次代码层提审驳回率 | 模板小程序提前由"附件通"主体试跑工具→办公审核；通过后将代码模板复用给作者小程序；首次种子作者跑 5-10 单实测驳回率 |
| **R5. fastregisterpersonalmp 频次上限** | 🟢 极低（前期）| 100 次/天、5 次/分钟/小程序 | 前期容量充足；规模化（>50 个/天新增作者）时申请配额或做注册队列 |
| **R6. 模板小程序代码升级影响所有授权方** | 🟡 低 | 一次代码 bug 可能影响所有作者的小程序 | 模板按版本灰度发布；每次更新先在自有测试小程序跑 24 小时 |
| **R7. 微信平台政策变化** | 🟠 中 | "流量主代运营"是新机制，未来政策可能调整 share_ratio 上限、权限集、个人小程序流量主资格 | 订阅微信开放社区公告；保持与服务商客服联系；架构上**所有微信相关参数都做配置化**，不硬编码 |
| **R8. 作者侧网络/合规风险** | 🟢 极低 | 作者上传违规文件被腾讯封禁连带服务商账号 | 强制 `mediaCheckAsync` 全文件机审；高敏感关键词文件名 + 内容双重校验；建立"作者违规一次警告、二次封禁"机制 |

---

## 13. 项目目录约定

- `_docs/` — 官方文档原文提取（`solution.txt` `guide.txt`），开发参考用，禁止删除。
- `CORE_ARCHITECTURE.md` — 本文档（V2.8 为当前生效版）。
- `CORE_ARCHITECTURE.v1.0.bak.md` — V1.0 备份，仅供回溯。

---

## 14. 已知待补齐项（不阻塞开发启动，但需排期跟进）

- [ ] 模板小程序的具体广告位 UX 设计（开屏频率、激励视频触发点、插屏频次上限）
- [ ] 作者侧 H5 入驻页面 UI 设计稿
- [ ] 平台后台 UI 设计稿
- [x] ~~HTML 卡片在公众号编辑器（Chrome/Edge）中的实际呈现验证~~（**V2.7 已过**：上一代项目「公众号附件助手」已工程实证作者在 Chrome/Edge 粘贴 `weapp_text_link` HTML 可正常渲染为小程序文本超链接）
- [x] ~~不同主体公众号文章嵌入个人主体小程序卡片~~（**V2.7 已过**：同上，主链路走 `weapp_text_link` 文本超链接，不走关联通道，不受不同主体限制）
- [ ] `api_getsettlement` 接口数据延迟实测（验证实时/T+1/T+半月哪种口径）
- [ ] **作者授权链接需同时包含代码管理权限集**（验证实际权限集 ID，避免授权后部署流水线断层）
- [ ] **个人主体作者首次提审驳回率实测**（5-10 个种子作者跑一轮，处理并调优隐私协议与 UGC 声明）
- [ ] **隐私保护指引黄金版本过审**（用附件通主体小程序跑一次全路径审核，固化为后续作者部署的黄金版本）
- [ ] 推广员二维码/短链规则与防伪机制
- [ ] 正式域名采购与 ICP 备案
- [ ] 腾讯云 COS Bucket 与 CDN 加速预约（V2.0 存储阶段）
- [ ] 模板小程序作为"附件通"主体试跑工具类目审核（验证个人主体作者同类目可过）

---

## 15. ⭐ 第一期 MVP 范围与迭代路线图（V2.4 开发期"圣经页"）

> **本节是开发期最高优先级参考。每天开发任何功能前，先用这一节检验"我在做的事是否在 P0 里"，防止过度设计。**

### 15.1 MVP 设计原则

1. **架构夯实**：核心数据模型（角色叠加位、`user_role_history`、`parent_promoter_user_id`、上绑定关系）一上线就站定，以后不需迁移。
2. **功能精简**：打通主链路即可，次要功能全部延后。
3. **运营自动化优先于作者体验**：一人运营背景下，"主理人只点两个按钮"比"作者一键注册"优先级更高。
4. **作者入驻人肉兑现**：推广员远程指导 + 详细图文教程，不走 `fastregisterpersonalmp` 接口集成。
5. **醒目颜色提示**：代码里任何「实现了不在 P0 中的功能」，PR 标题打 `[P1]`，费时者自负。

### 15.2 P0 必做清单（第一期上线必须含）

#### 15.2.1 后端核心数据模型

| 表 | 用途 |
|---|---|
| `users` | B 端用户统一表；含 `is_promoter` / `is_admin` / `parent_promoter_user_id` / `bound_appid` / `authorizer_refresh_token` 等字段 |
| `user_role_history` | 角色位变更历史（铁律 7，预留按曝光日结算能力）|
| `files` | 附件元信息 + COS 路径 + mediaCheck 状态 |
| `file_share_cards` | 生成的复制内容快照（文本超链接 / 小程序卡片 / H5 备份三种 HTML 形式）|
| `authorizations` | 作者小程序授权记录（appid + share_ratio + 变更历史）|
| `daily_ad_stats` | 每个授权小程序每日广告数据（从 `api_getadposdetail` 拉）|
| `settlement_records` | 每个授权小程序每期结算数据（从 `api_getsettlement` 拉）|
| `commission_settlements` | 推广员佣金结算单（含 `is_self_promoter`、状态机、打款凭证）|
| `wx_tokens` | `component_access_token` 与 `authorizer_access_token` 中央存储（含过期时间、负责自动刷新）|
| `mp_template_versions` | 模板小程序代码版本管理（template_id、user_version、发布状态、变更日志）|
| `mp_deployments` | 作者小程序部署记录（含部署状态机、ext_json 快照、部署事件流）|
| `mp_audits` | 提审记录（含审核状态、驳回原因、分流标签、重试次数）|
| `admin_action_log` | 主理人关键操作审计日志 |

#### 15.2.2 微信 API 集成（运营自动化的中枢）

| API | 调用频率 | 作用 |
|---|---|---|
| `getAuthorizerList` | 每日 02:00 | 同步已授权作者列表，检测新增/取消授权 |
| `getAuthorizerInfo` | 授权变更时 | 获取作者小程序详情 |
| `SetShareRatio` | 服务初始化一次 + 差异化设置时 | 设默认 28%，个别作者可差异化 |
| `AgencyCreateAdunit` | 作者授权完成时 | 自动为作者小程序创建默认广告位 |
| `api_getadposdetail` | 每日 03:00 | 拉取前一天每个授权小程序的广告数据 |
| `api_getsettlement` | 每月1/16号 04:00 | 拉取上一结算期的收入数据 |
| `api_getagencysettlement` | 每月1/16号 04:30 | 拉取服务商汇总结算数据，对账用 |
| `mediaCheckAsync` | 作者上传附件时 | 机审，合规必要 |
| `wxa/add_category` | 作者授权后 | 自动设置「工具 → 办公」类目（V2.6 实测可批） |
| `wxa/modifyserverdomain` | 作者授权后 | 配置服务器域名（平台后端 + COS）|
| `wxa/setMpPrivacySetting` | 作者授权后 | 设隐私保护指引（黄金版本）|
| `wxa/commit` | 作者授权后 + 模板版本发布 | 上传代码到作者小程序开发版，注入 ext_json |
| `wxa/get_page` | 提审前 | 获取小程序页面列表 |
| `wxa/submit_audit` | commit 后 | 提交审核（含 ugc_declare、privacy_api_not_use、preview_info）|
| `wxa/release` | 审核通过后 | 发布上线 |
| `wxa/undocodeaudit` | 必要时 | 撤回审核 |
| `wxa/revertcoderelease` | 紧急事件 | 版本回退 |
| `wxa/get_qrcode` | 体验需要时 | 获取体验版二维码 |
| **事件推送接收** | 实时 | 接收授权/审核成功/驳回/延期等事件 |
| `fastregisterpersonalmp` | — | **MVP 不集成**，V1.5 再接 |

**安全设计**：
- `component_access_token` 由后端中枢服务统一维护，提前 5 分钟调用刷新接口。
- `authorizer_access_token` 为每个作者小程序独立维护，使用 `authorizer_refresh_token` 刷新。
- API 调用全部包装为同一个 `WxOpenClient` SDK 封装层，底层处理 token 获取/刷新/重试/限流/错误码。上层业务代码不关心 token 生命周期。

#### 15.2.3 作者端功能

| 功能 | MVP 实现范围 |
|---|---|
| H5 入驻入口 | 扫推广员二维码进入 H5、微信扫码登录、获 unionid、绑定推广关系 |
| 入驻指引页（V2.8）| 详细图文 + 视频教作者在 `mp.weixin.qq.com` 自助注册个人主体小程序（身份证 + 人脸 + 手机号）；不会注册的作者点「需要帮助」后推广员远程屏幕共享/电话指导 |
| appid 回填 | 作者把刚注册的小程序 appid 填入 H5 表单提交 |
| 作者授权 | 跳转微信授权 H5（新版，同时勾选权限集 135 代运营 + 代码管理权限集 + share_ratio=28）→ 作者确认 → 后端收到授权事件后续 |
| PC Web 上传附件 | 拖拽上传 → 腾讯云 COS 临时凭证直传 → 调 `mediaCheckAsync` → 生成富文本（`weapp_text_link` 文本超链接为主）→ 一键复制 |
| 作者小程序工作台 | 我的附件列表 / 上下架 / `wx.chooseMessageFile` 替换 / 流量主收益查看 |

#### 15.2.4 C 端模板小程序

| 功能 | MVP 实现范围 |
|---|---|
| 附件预览 | PDF/Word/Excel/图片基本预览（腾讯云文档预览能力）|
| 下载 | 触发激励视频广告 → 观看完解锁下载 |
| 广告位预埋 | 开屏广告 + 激励视频 + 插屏三种位预埋 |
| UV 上报 | 上报到平台后端用于门槛检测 |

#### 15.2.5 推广员能力

| 功能 | MVP 实现范围 |
|---|---|
| 推广员开启 | 主理人后台 toggle `is_promoter`（不做申请制）|
| 二维码生成 | 开启时自动生成（含 promoter_id）|
| 推广数据 | 名下作者列表 + 本期预估佣金 + 历史结算单 |
| 收款设置 | 填入收款微信号/银行卡信息（主理人转账时查阅）|

#### 15.2.6 主理人后台（一人版极简）

一个仪表盘 + 一个待办列表 + 几个查询页面：

```
【1】仪表盘
本月广告毛收益总盘 / 平台净收入 / 待付推广员佣金 / 授权作者数

【2】待办列表（主理人唯一日常入口）
⚠ N 张推广员结算单待审核              [立即审核]
⚠ N 张已审核待打款                     [去打款]
⚠ N 个文件 mediaCheck 返回违规需复审   [复审]

【3】查询页面
作者列表 / 推广员管理 / 历史结算记录 / API 同步状态 / share_ratio 设置
```

#### 15.2.7 铁律执行表（哪些必做、哪些延后）

| 铁律 | P0 | 说明 |
|---|---|---|
| 1. 按广告曝光日逐日归属 | ✅ **简化为按结算期归属** | 按半月结算周期计，`user_role_history` 必存以备 V1.5 升级逐日 |
| 2. 单层提成、禁多层 | ✅ 必做 | 合规红线不可谈判 |
| 3. CHECK + 递归防循环 | ✅ 必做 | DDL 一行约束 |
| 4. 中间件实时鉴权 | ✅ 必做 | web 安全底线 |
| 5. 主理人自营打标 | ✅ 必做 | 一个字段的事 |
| 6. 30 天冷却期 | ⛔ **延后**到 V1.5 | MVP 不开放申请制 |
| 7. 角色变更入历史表 | ✅ 必做 | 支撑铁律 1 |
| 8. 申请快照统计数据 | ⛔ **延后**到 V1.5 | MVP 不开放申请制 |

#### 15.2.8 模板小程序生命周期管理（V2.5 新增 P0 核心能力）

服务商身份的最大价值——作者一次授权后，平台**自动**完成该作者小程序的全生命周期托管。主理人**只在驳回与异常时介入**。

##### 自动化流水线

```
作者授权完成（事件推送到平台）
   │
   ▼
【阶段 1：基础配置】（10 秒内完成，5 个 API 顺序调用）
   ├─ SetShareRatio(28)
   ├─ AgencyCreateAdunit × 3（开屏/激励视频/插屏）
   ├─ wxa/add_category（工具→办公）
   ├─ wxa/modifyserverdomain（指向平台后端 + COS）
   └─ wxa/setMpPrivacySetting（隐私保护指引黄金版本）
   │
   ▼
【阶段 2：代码部署】（wxa/commit）
   注入 ext_json，含：
   ├─ extAppid（作者小程序 appid）
   ├─ ext.platformUserId（作者在附件通的 user_id）
   ├─ ext.authorName（作者品牌名）
   ├─ ext.primaryColor（主题色）
   ├─ ext.adUnitIds（专属广告位 ID）
   ├─ ext.apiBaseUrl
   └─ ext.privacyVersion
   │
   ▼
【阶段 3：提审】（wxa/get_page → wxa/submit_audit）
   ├─ item_list（页面与类目映射，平台预定）
   ├─ version_desc（"附件通 v1.0 首版"）
   ├─ feedback_info（平台统一客服联系方式）
   ├─ feedback_stuff（演示视频/截图 URL，平台预备）
   ├─ preview_info（截图/录屏，N 个作者复用同一组）
   ├─ ugc_declare（黄金措辞：作者上传 + 平台审核机制）
   └─ privacy_api_not_use（隐私 API 声明，代码扫描自动维护）
   │
   ▼
【阶段 4：等待审核结果（事件推送驱动）】
   ├─ weapp_audit_success → 自动调 wxa/release → 标记"已上线"
   ├─ weapp_audit_fail → 解析驳回原因 → 状态机分流：
   │     ├─ "代码"关键字 → 状态：等待模板修复
   │     ├─ "资质/类目/主体"关键字 → 状态：等待作者补料 + 推送通知
   │     └─ 不明原因 → 加入"待人工处理"队列
   └─ weapp_audit_delay → 等待
   │
   ▼
【阶段 5：上线】（wxa/release）
   作者状态 = "已上线运行中"
   UV 累积 → 满 1000 自动开通流量主
```

##### 差异化策略：模板代码 1 套，配置 N 套

```json5
// 部署到作者 A 时的 ext_json
{
  "extEnable": true,
  "extAppid": "wxabc123...",
  "ext": {
    "platformUserId": "uid_456",
    "authorName": "李雷的工作台",
    "primaryColor": "#3B82F6",
    "adUnitIds": {
      "splash": "adunit_xxx",
      "rewardedVideo": "adunit_yyy",
      "interstitial": "adunit_zzz"
    },
    "apiBaseUrl": "https://api.fujiantong.com",
    "customerServiceWxId": "fjt_service",
    "privacyVersion": "v2.1"
  },
  "window": {
    "navigationBarTitleText": "李雷的工作台"
  }
}
```

代码内统一 `wx.getExtConfigSync()` 读取——**模板 bug 修一次，全员生效**。

##### 部署状态机

每个作者小程序 `mp_deployments.status` 状态机：

```
   pending          ──授权事件触发──>   configuring
configuring        ──基础配置完成──>   committing
committing         ──代码部署完成──>   submitted_for_audit
submitted_for_audit──事件: 审核通过──>   ready_to_release
ready_to_release   ──release 完成──>   live
submitted_for_audit──事件: 审核驳回──>   audit_failed
audit_failed       ──分流──>           waiting_template_fix / waiting_author_supplement / manual_review
waiting_template_fix──模板新版到来──>  committing（重新提审）
waiting_author_supplement──作者补料完成──> committing
manual_review      ──主理人手动操作──>  committing / cancelled
live               ──模板版本升级──>   committing（升级流水）
```

##### 6 个工程坑及缓解策略

| 坑 | 缓解策略 |
|---|---|
| 1. 个人主体首次提审驳回率高（30-50%）| 模板隐私协议、用户协议、UGC 声明先用平台主体试跑过审作为黄金版本；类目锁"工具→办公"（V2.6 主理人后台实测可批） |
| 2. `preview_info` 截图录屏怎么自动生成 | 预备 1 套截图/录屏（关键页面），存 COS，所有作者提审复用同一组 URL |
| 3. `ugc_declare` 与 `privacy_api_not_use` 必须精准 | 抽到代码常量层；模板版本变更触发声明字段更新检查 |
| 4. 驳回处理状态机复杂 | 关键词分流（"代码"/"资质"/"其他"）+ 自动重试 + 人工兜底队列 |
| 5. 发布频次限制（紧急修复全量推送可能撞限流）| 后端发布队列（Redis），按"近 7 天活跃"优先 + 滚动发布 |
| 6. `user_version` 版本号管理 | 模板版本 + 作者后缀（如 `1.0.3-uid456`）避免冲突 |

##### 主理人后台「部署管理」页面

```
═══════════════════════════════════════════════════════
小程序部署管理                              [v1.0.3]
═══════════════════════════════════════════════════════

⚠ 待办（3 项）
   3 个作者审核驳回，需查看原因         [立即查看]
   2 个作者授权后流程异常                [立即处理]

部署状态总览（共 47 个授权作者）
─────────────────────────────────────────
🟢 已上线运行中                  38
🟡 审核中                         4
🔴 审核驳回（代码问题待模板修复）  2
🔴 审核驳回（作者资质问题待补料）  1
🟠 部署进行中                     1
🟠 等待人工处理                   1

模板版本管理
─────────────────────────────────────────
当前模板：v1.0.3（修复 PDF 预览闪退）
已部署：38/47 个作者
[发布新版]  [回滚]  [查看版本历史]

近 24 小时事件流
─────────────────────────────────────────
14:23  作者「李雷」小程序部署成功 ✅
12:48  作者「韩梅梅」首次提审通过 ✅
10:15  作者「王二」审核驳回（隐私协议不全）⚠ [查看]
08:32  作者「赵六」commit 失败：版本号冲突 ❌ [重试]
```

##### 关键前提（V1.0 上线前必须验证）

**作者授权链接的权限集组合**：除了流量主代运营权限集（135），授权链接还必须**同时勾选小程序代码管理权限集**（典型为 17，需在微信开放平台第三方平台后台确认实际 ID）。如果作者只授权 135 不授权代码管理权限集，**整条流水线断**。

授权链接拼接时把所需权限集 ID 用逗号拼在一起——作者**一次授权同意**两组权限。

### 15.3 P1 延后清单（V1.5 迭代）

- `fastregisterpersonalmp` 接口集成（一键代注册个人小程序）
- 推广员申请制 + 主理人审核流程
- N/M 门槛配置与「解锁申请」逻辑
- 30 天冷却期 + 申请快照表
- 推广收益按广告曝光日逐日归属
- 作者晋升佣金增长跟踪
- 广告位动态调优（A/B 测试不同位置对收益的影响）
- 模板小程序版本灰度发布（分批推送 30%→70%→100%）
- 驳回原因智能分类与推荐修复方案
- 代码扫描自动生成 `privacy_api_not_use` 声明

### 15.4 P2 长期或不做清单

- 复杂违规申诉流程（V2.0+）
- 数据看板可视化（V2.0+）
- 多语言支持（不做）
- 多层级佣金体系（永远不做，合规红线）
- 微信支付分账接口（不适用场景，不做）
- 自建 CDN（不做，用腾讯云 COS）

### 15.5 上线节奏（V2.5 调整为 12.5 周）

| 周次 | 产出 |
|---|---|
| W1-W2 | 数据库 schema + 后端骨架（Express/Fastify/Nest）+ 微信开放平台授权联调 |
| **W3-W3.5** | **微信 API SDK 封装层**：token 管理 + 所有 API 统一入口 + cron 调度器 + **小程序生命周期 SDK（commit/submit_audit/release 全套）** + 事件推送接收服务 |
| W4-W5 | 模板小程序（含预埋广告位 + ext_json 配置读取）+ 授权事件响应服务 |
| W6 | 平台主体小程序试跑审核（隐私黄金版本 + UGC 声明 + 类目设置过审）作为后续模板部署基础 |
| W7 | PC Web 作者工作台 + H5 入驻流程 |
| W8 | 主理人后台（仪表盘 + 待办 + 查询 + **部署管理页**）|
| W9 | 全链联调 + 首个作者代部署端到端走通（从 appid 回填到小程序上线全流程）。**weapp_text_link 文本超链接公众号渲染验证已由上代项目「公众号附件助手」完成，本周不再作为生死点验证项** |
| W10 | 5-10 个种子作者灰度上线，重点验证首次提审驳回率 |
| W11 | 根据驳回反馈调优模板隐私协议/UGC 声明；代码 bug 修复 |
| **W12** | 补漏、错位修复、正式公开推广 |

### 15.6 主理人月度运营工作量预估

| 频次 | 动作 | 预估耗时 |
|---|---|---|
| 每月 1 日、16 日 | 收到结算单生成通知 → 看仪表盘 → 逐个审核 → 微信转账推广员 → 回填打款凭证 | 15 分钟 |
| 每月一次 | 给腾讯邮寄增值税专票（领取平台 28% 收益必要流程）| 30 分钟 |
| 不定期 | 反馈中心看作者疑问 → 回复；推广员推荐后走后台 toggle is_promoter | 10 分钟/人 |
| **月总工作量** | | **约 1-2 小时** |

### 15.7 仍需手动的动作（无法自动化）

| 动作 | 原因 | 频率 |
|---|---|---|
| 主理人人工转账推广员 | 微信支付分账不允许本场景 | 每半月 |
| 平台向腾讯邮寄增值税专票 | 腾讯财务流程不提供 API | 每月 |
| 作者创建自己的小程序 | MVP 不集成 fastregisterpersonalmp | 作者首次入驻时 |
| 高风险附件复审 | mediaCheck 不能唯一权威 | 不定期 |

### 15.8 成功判定指标（V1.0 上线 30 天后检查）

- [ ] 注册作者 ≥ 20
- [ ] 附件发表量 ≥ 100 份
- [ ] `weapp_text_link` 文本超链接在公众号推文中渲染成功率 = 100%
- [ ] C 端广告曝光 ≥ 10000 次
- [ ] 至少 5 个作者小程序 UV 达到 1000 并成功开通流量主
- [ ] 推广员结算单首期生成 + 转账成功
- [ ] 主理人月运营工作量 < 2 小时

指标达成则进入 V1.5 迭代；任何一项不达标先诊断原因、修复后再迭代。

---

**版本控制**
- V1.0（已弃用，备份保留）— 初始立项；错误地认为"自动分账不可行"
- V2.0 — 基于官方"流量主代运营"机制重写；明确 share_ratio=28、半月自动分账、模板预埋策略、入驻五步法
- V2.1 — 基于市场与官方文档对抗性验证修订：
  - 客群锁定"个人运营者"
  - 排除 `fastregisterweapp` 企业代注册路径（仅允许线下类目）
  - 新增 4.5 节"HTML 卡片直达"绕开公众号关联负担（V2.7 后修正：主链路为 `weapp_text_link` 文本超链接）
  - 入驻五步法校准为基于 `fastregisterpersonalmp` 的真实流程
  - 新增第 11 节竞品分析与第 12 节业务风险表
- V2.2 — 角色模型重塑：
  - 第 3 节完全重写：废除"4 类独立角色"模型，确立"基础角色（作者）+ 叠加权限（推广员/管理员）"模型
  - 明确所有 B 端用户共用一张 `users` 表 + 标志位；**禁止建独立的 promoters 表**
  - 前期推广员由主理人手动添加，预留"满足数据门槛自动升级"的成长路径开关（暂不开放）
  - 小程序端统一为"作者工作台"，根据 `is_promoter` 字段动态展示"推广收益" Tab
  - 第 3.5 节确立"用户成长闭环"作为产品自然裂变机制
- V2.3 — 角色模型加固与防错乱铁律：
  - 3.4 节推广员获取机制由"自动升级"改为"申请制"：门槛仅解锁申请按钮，升级需主理人手动审核通过
  - 3.6 节小程序 UI 增加"申请成为推广员"条件入口
  - 新增 3.7 节"角色模型铁律" 8 条防错乱红线
- V2.4 — MVP 范围与全自动化路线图：
  - 明确"主理人一人运营"约束，运营端动作最大化自动化
  - 顶部新增 V2.4 修订说明，明确 MVP 边界
  - **新增第 15 节"第一期 MVP 范围与迭代路线图"**（V2.4 开发期"圣经页"）：
    - 15.1 MVP 设计原则
    - 15.2 P0 必做清单（数据模型 / 微信 API 集成 / 作者端 / C 端 / 推广员 / 主理人后台 / 铁律执行表）
    - 15.3 P1 延后清单（V1.5 迭代）
    - 15.4 P2 长期或不做清单
    - 15.5 上线节奏 11 周
    - 15.6 主理人月度运营工作量预估（约 1-2 小时/月）
    - 15.7 仍需手动的动作清单
    - 15.8 V1.0 成功判定指标
  - 第 14 节加入 `api_getsettlement` 数据延迟实测项
  - 关键决策：MVP 不集成 `fastregisterpersonalmp`、推广员佣金按结算期归属（非按曝光日）、申请制延后 V1.5
- **V2.5 — 模板小程序全生命周期自动化**：
  - 服务商身份能力充分挖掘：作者一次授权 → 平台自动完成「配置/部署/提审/发布/上线」全流程
  - 模板代码 1 套、配置 N 套：差异化全部走 `ext_json` 注入，模板 bug 修复一次推送全员
  - **新增 15.2.8 节「模板小程序生命周期管理」**（P0 核心能力）：
    - 5 阶段自动化流水线（基础配置 → 代码部署 → 提审 → 等结果 → 上线）
    - ext_json 差异化注入策略
    - 部署状态机（11 个状态 + 关键转移）
    - 6 个工程坑（驳回率/preview_info/声明字段/状态机/限流/版本号）+ 缓解策略
    - 主理人后台「部署管理」页面（含模板版本管理 + 待办 + 事件流）
    - 关键前提：作者授权链接需同时勾选代码管理权限集
  - 15.2.1 增加三张表：`mp_template_versions`、`mp_deployments`、`mp_audits`
  - 15.2.2 集成 12 个新 API：`wxa/add_category`、`wxa/modifyserverdomain`、`wxa/setMpPrivacySetting`、`wxa/commit`、`wxa/get_page`、`wxa/submit_audit`、`wxa/release`、`wxa/undocodeaudit`、`wxa/revertcoderelease`、`wxa/get_qrcode`、事件推送接收 等
  - 14 节增 3 项验证待补齐：授权链接权限集组合、个人主体首次提审驳回率、隐私保护指引黄金版本过审
  - 15.5 上线节奏 11 周 → 12.5 周：W3 扩展为「W3-W3.5 微信 API SDK + 小程序生命周期 SDK」、新增 W6 主体试跑审核 + W11 驳回调优
  - 15.3 P1 增加：模板灰度发布、驳回智能分类、`privacy_api_not_use` 代码扫描生成 三项
- **V2.6 — 类目校准 + 证据等级铁律**：
  - 类目修正：「工具→效率工具」是 AI 臆造分类。主理人后台实测——个人主体小程序可批「工具→办公」「工具→信息查询」「商业服务→企业管理」等类目，本项目模板小程序锁定主营类目为「工具→办公」
  - 确立证据等级铁律：A 级（官方接口文档/微信团队署名公告/主理人实测）必采，B 级（社区开发者帖）仅作线索，C 级（网友/自媒体）禁用
  - R4 风险降级为「已实证可批，残余风险为首次代码层提审驳回率」
- **V2.7 — HTML 直达机制工程实证校准**：
  - 主链路从 `<mp-miniprogram>` 卡片校准为 `<a class="weapp_text_link js_weapp_entry">` 文本超链接。V2.1 至 V2.6 一直误标了示例形式，本版根据主理人提供的上一代项目「公众号附件助手」代码做了根本性更正
  - `<mp-miniprogram>` 标签明确仅用于公众号自动回复/服务号推送等辅助场景
  - 一键复制三场景锁定：一键粘贴（主，weapp_text_link）/ 卡片代码（辅，mp-miniprogram）/ H5 备份（兼底）
  - 「不同主体公众号嵌入个人主体小程序卡片」生死点被上代项目代码实证打破
  - 第 4.5 节大改：新增 4.5.1～4.5.5 五个子小节（HTML 结构 / 三级回退 / 操作流程 / 辅助场景 / 跳出关联限制原理）
  - R3 风险降级为「极低（V2.7 已工程实证解决）」
  - 14 节相关两个生死点项目划掉
  - MVP 可复用上一代代码作为后台起点
- **V2.8（当前生效版）— 入驻路径终极锁定 + V2.7 后通读校对**：
  - **入驻路径终极锁定为「作者自助注册 + 自动化授权」**：作者在 mp.weixin.qq.com 自助注册个人主体小程序（推广员可远程协助），不接 `fastregisterpersonalmp` 接口；注册完成后 appid 回填，后续授权/配置/部署/提审/发布全自动化
  - 5.1 节五步法改为六步法：①推广员链接 ②扫码登录 ③**作者自助注册（推广员可协助）** ④**回填 appid + 一键授权** ⑤银行卡绑定 ⑥**自动化部署**
  - 5.1.1 反面警示更新：明确 V2.8 决策不接 `fastregisterpersonalmp` 的产品原因（人脸需本人、作者悘挫率高），「复用公众号主体」标注为「作者本人手动可选但与个人主体默认不兼容」
  - 通读校对修正 14 处问题：V2.1 mp-miniprogram 残留加 V2.7 校正标注、修订说明版本顺序调整（V2.6 → V2.7 → V2.8）、R3/R4 表格字符乱码修复、接口名 `GetSettlement`→`api_getsettlement` 统一、3.2/10/11.2 节「HTML 卡片」表述精准化、W9 周生死点同步删除、15.8 成功指标更名为 `weapp_text_link`、5.1 实名认证表述去 fastregisterpersonalmp 提及、接口表 fastregisterpersonalmp 加「V1.5 接入」标注、笔误三处（打打→打破、师弟→晋升、骨骨补丢→补漏）、底部版本控制清单补 V2.6/V2.7/V2.8 条目、第 13 节项目目录"V2.1+ 唯一权威版本"改为"V2.8 为当前生效版"
